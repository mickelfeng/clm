// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("clm", "for clm support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("clm", "enable clm support", "no");

if (PHP_CLM != "no") {
	EXTENSION("clm", "clm.c");
}

